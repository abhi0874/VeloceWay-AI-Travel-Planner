[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Route /api/* to the Netlify function wrappers (plan + suggest)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
